{
    "require": {
        "php": ">=5.3.1"
    },
    "minimum-stability": "dev",
    "prefer-stable" : true,
    "scripts": {
        "pre-install-cmd": "Deepeloper\\\\ComposerXML::prePackageInstall"
    }
}
